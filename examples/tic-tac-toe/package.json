{
    "name": "@examples/tic-tac-toe",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev:example": "vite",
        "build:example": "tsc && vite build",
        "build:cloudflare": "tsc && vite build --mode cloudflare",
        "preview": "vite preview"
    },
    "dependencies": {
        "@grainular/grains": "^2.0.0",
        "@grainular/nord": "^2.0.0"
    },
    "devDependencies": {
        "typescript": "^7.0.2",
        "vite": "^8.2.1"
    }
}
